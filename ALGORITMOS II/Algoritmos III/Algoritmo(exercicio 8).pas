Program exercicio8;
 var Xh,Xmin,x:integer;
Begin
	writeln('escreva um valor que corresponde as horas' );
	readln(Xh);
	writeln('escreva um valor que corresponde aos minutos');
	readln(Xmin); 
	x:=Xh*60+Xmin;
	writeln('resultado=' ,x)
  
End.